@model AllowedEmailDomainUpsertDto

@{
    ViewData["Title"] = @localizer["Allowed Email Domain"];
    ViewData["SubTitle"] = Model.Id == null ? @localizer["Add New Allowed Email Domain"] : Model.DomainName;
    ViewData["ActiveSidebar"] = "liAllowedEmailDomain";
}

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12" style="margin: auto !important;">
                <div class="card card-outline card-primary">
                    <form asp-action="@(Model.Id == null ?"CreateAllowedEmailDomain":"UpdateAllowedEmailDomain")" method="post">
                        <input type="hidden" asp-for="Id" />
                        <div class="card-body">
                            <div class="row">
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label asp-for="DomainName"> @localizer["Allowed Email Domain Name"]</label><span class="text-red text-bold">*</span>
                                        <input asp-for="DomainName" class="form-control" />
                                        <span asp-validation-for="DomainName" class="text-danger"></span>
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label> @localizer["Activety Status"]</label>
                                        <select asp-for="IsActive" class="form-control" style="width: 100%;">
                                            <option value="false">@localizer["Inactive"]</option>
                                            <option value="true">@localizer["Active"]</option>
                                        </select>
                                    </div>
                                </div>
                            </div>

                            <div class="space-4"></div>
                            <div class="row">
                                <div class="col-sm-12 pr-10">
                                    <div class="float-right">
                                        <a id="btnClose" asp-action="Index" class="btn btn-sm btn-secondary m-1 pl-3 pr-3"><i class="ace-icon fa fa-arrow-left"></i> @localizer["BackToList"]</a>
                                        @if (Model.Id != null)
                                        {
                                            <a asp-action="DeleteAllowedEmailDomain" asp-route-id="@Model.Id" onclick="return confirm('@localizer["Are you sure you want to delete this Allowed Email Domain?"]')" class="btn btn-sm btn-danger m-1 pl-3 pr-3"><i class="ace-icon fa fa-trash bigger-130 "></i> @localizer["Delete"]</a>
                                        }
                                        <button type="submit" class="btn btn-sm btn-primary"> @(Model.Id == null ? @localizer["Save"] : @localizer["Edit"])</button>

                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {

    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}




